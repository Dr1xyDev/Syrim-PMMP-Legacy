<?php
/*
 * Syrim - Port de Nukkit 1.1.5 PopulatorMineshaft (placeholder - WIP en Nukkit original)
 */

namespace pocketmine\level\generator\nukkit\populator;

use pocketmine\level\ChunkManager;
use pocketmine\utils\Random;

class PopulatorMineshaft extends Populator {

    /**
     * Syrim: el original de Nukkit es WIP (TODO), por lo que esta clase
     * es un placeholder que no genera nada todavía.
     * Se deja para que los biomas puedan referenciarla sin error.
     */
    public function populate(ChunkManager $level, int $chunkX, int $chunkZ, Random $random) {
        // WIP - sin implementación
    }
}
